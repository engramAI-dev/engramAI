# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.10.0
pydantic-settings>=2.6.0

# Database
sqlalchemy>=2.0.36
asyncpg>=0.30.0
# psycopg2 is used only by alembic migrations (sync env); the app runs async on asyncpg.
psycopg2-binary>=2.9.10
alembic>=1.14.0
pgvector>=0.3.6

# Auth
httpx>=0.28.0
python-jose[cryptography]>=3.3.0

# LLM (D45: configurable provider)
google-genai>=1.0.0    # Gemini (free tier, default)
anthropic>=0.39.0      # Claude (paid, set LLM_PROVIDER=anthropic)
sentence-transformers>=3.0.0

# Code post-processing (B3)
# tree-sitter pinned: tree-sitter-languages 1.10.x ships grammars compiled
# against the pre-0.22 tree-sitter ABI; newer tree-sitter breaks grammar load.
tree-sitter==0.21.3
tree-sitter-languages>=1.10.2

# Task queue
celery[redis]>=5.4.0

# MCP plugin (B14)
mcp>=1.0.0

# Utilities
python-dotenv>=1.0.1
python-multipart>=0.0.12

# Dev / Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0
ruff>=0.8.0