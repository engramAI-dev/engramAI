# Web framework
fastapi>=0.136.0
uvicorn[standard]>=0.46.0
pydantic>=2.13.0
pydantic-settings>=2.14.0

# Database
sqlalchemy>=2.0.49
asyncpg>=0.31.0
psycopg2-binary>=2.9.12
alembic>=1.18.0
pgvector>=0.4.2

# Auth
httpx>=0.28.1
python-jose[cryptography]>=3.5.0

# LLM (D45: configurable provider)
google-genai>=1.74.0     # Gemini (free tier, default)
anthropic>=0.97.0        # Claude (paid, set LLM_PROVIDER=anthropic)

# Embeddings (D44: local model)
sentence-transformers>=5.4.0
einops>=0.8.2            # required by nomic-embed model

# Code post-processing (B3)
# tree-sitter pinned: tree-sitter-languages 1.10.x ships grammars compiled
# against the pre-0.22 tree-sitter ABI; newer tree-sitter breaks grammar load.
tree-sitter==0.21.3
tree-sitter-languages>=1.10.2

# Task queue
celery[redis]>=5.6.0

# MCP plugin (B14)
mcp>=1.0.0

# Utilities
python-dotenv>=1.2.0
python-multipart>=0.0.12

# Dev / Testing
pytest>=9.0.0
pytest-asyncio>=1.3.0
ruff>=0.15.0
